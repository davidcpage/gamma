from setuptools import setup

setup(
    name='gamma',
    version='0.0.0',
    description='Pattern matching and rewriting for computation graphs.',
    url='https://github.com/davidcpage/gamma',
    author='David Page',
    packages=['gamma'],
    install_requires=['pydot'],
    python_requires='>=3',
    )

